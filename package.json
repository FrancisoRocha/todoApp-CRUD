{
  "name": "todoapp-crud",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "npx json-server --watch server/db.json --port 3000 --host localhost",
    "dev:full": "concurrently \"npm run server\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "sweetalert": "^2.1.2",
    "uuid": "^11.1.0"
  }
}
